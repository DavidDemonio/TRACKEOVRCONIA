{
  "name": "trackeovrconia",
  "private": true,
  "packageManager": "pnpm@8.15.3",
  "scripts": {
    "dev": "pnpm -r --stream dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter server start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "pnpm -r format"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools"
  ]
}
